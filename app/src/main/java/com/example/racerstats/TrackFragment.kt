package com.example.racerstats

import com.example.racerstats.track.TrackFragment as TrackFeatureFragment

/**
 * Legacy alias kept so existing navigation still resolves to the new track feature fragment.
 */
class TrackFragment : TrackFeatureFragment()